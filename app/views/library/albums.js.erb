$('#top_albums').html("<%= j render 'library/partials/albums' %>")
$('#albums_paginate').html("<%= j paginate @albums %>")

$('#top_albums_scope').html("<%= j render 'library/scope/albums' %>")
$('#top_albums_order').html("<%= j render 'library/order/albums' %>")

$('.dropdown.open .dropdown-toggle').dropdown('toggle')

url = "<%= library_albums_path(order: params[:order], page: params[:page], scope: params[:scope]) %>".replace('amp;', '')
window.history.pushState({turbolinks: true, url: url}, '', url)