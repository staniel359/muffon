Order:
- if params[:scope]
	- if params[:scope] == '1'
		most played
	- if params[:scope] == '2'
		least played
	- if params[:scope] == '3'
		newest
	- if params[:scope] == '4'
		oldest
- else
	most played
.caret