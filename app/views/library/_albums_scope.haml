.dropdown.inline
  .btn.dropdown_btn.dropdown-toggle{data: {toggle: 'dropdown'}} 
    = render 'library/scope_label', default_scope: @profile.top_albums_scope

  %ul.dropdown-menu.dropdown-menu-right
    %li= link_to '1 week', albums_scope_path(scope: 7), remote: true
    %li= link_to '1 month', albums_scope_path(scope: 30), remote: true
    %li= link_to '3 months', albums_scope_path(scope: 90), remote: true
    %li= link_to '6 months', albums_scope_path(scope: 180), remote: true
    %li= link_to '1 year', albums_scope_path(scope: 365), remote: true
    %li= link_to 'All time', albums_scope_path(scope: 'all'), remote: true

- if current_profile?(@profile)
  #top_albums_default_scope.inline
    - if params[:action] == 'albums_scope' && params[:scope].to_i != @profile.top_albums_scope
      = button_to 'Save', albums_default_scope_path(scope: params[:scope], default: true), method: :patch, remote: true, class: 'btn btn-primary'
    - if params[:default]
      = button_tag 'Saved', class: 'btn btn-primary disabled'