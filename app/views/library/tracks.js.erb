$('#tracks').html("<%= j render 'library/partials/tracks' %>")
$('#tracks_paginate').html("<%= j paginate @tracks %>")
$('#tracks_scope').html("<%= j render 'library/partials/order_label' %>")
$('.dropdown.open .dropdown-toggle').dropdown('toggle')

url = "<%= '?order=' + params[:order]%><%= '&page=' + params[:page] if params[:page] %>".replace('amp;', '')
window.history.pushState({turbolinks: true, url: url}, '', url)