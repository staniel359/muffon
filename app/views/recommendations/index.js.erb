$('#recommendation-query').html("<%= j render 'query' %>")
$('#recommendations').html("<%= j render 'recommendations' %>")
$('#loading').hide()
$('#upbar-wrap').html("<%= j render 'layouts/upbar' %>")
$('[data-toggle="tooltip"]').tooltip()

URL = "?<%= @query_params.to_query %>"
formattedURL = URL.replace(/"/g, "'").replace(/&amp;/g, "&");
window.history.pushState({turbolinks: true, url: formattedURL}, '', formattedURL)
