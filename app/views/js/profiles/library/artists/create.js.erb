$("#artist-library-action_<%= @object.id %> .action-btn").tooltip('hide')
$("#artist-library-action_<%= @object.id %>").html(
  "<%= j render 'partials/actions/library', artist: @object %>"
)
$('[data-toggle="tooltip"]').tooltip({trigger: 'hover'})
$("#artist-library-action_<%= @object.id %> .action-btn").tooltip('show')
