- if @album.labels.any?
  .align-center.mb-2.mobile-centered
    %i.fas.fa-dot-circle.mr-2
    - @album.labels.pluck(:name).each do |label_name|
      .mr-1
        = link_to label_name, label_path(label_name: label_name), class: 'btn btn-dark btn-sm'
