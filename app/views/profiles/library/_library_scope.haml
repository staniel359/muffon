.dropdown
  = button_tag class: 'btn-transp dropdown-toggle text-white', data: { toggle: 'dropdown' } do
    %i.far.fa-calendar-alt
    = scope_text(collection)
  .dropdown-menu.dropdown-menu-right
    - scopes_hash.each do |scope_value, scope_name|
      .dropdown-item
        = button_to scope_name, profiles_library_scope_path(collection: collection, scope: scope_value), class: 'btn-transp', method: :get, remote: true
